# 🛒 Lokali - Configuration d'environnement
# Copiez ce fichier vers .env et configurez vos paramètres

# ===========================================
# CONFIGURATION DE LA BASE DE DONNÉES
# ===========================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=localprice

# ===========================================
# CONFIGURATION DU SERVEUR
# ===========================================
PORT=5000
NODE_ENV=development

# ===========================================
# KOBO — WEBHOOK SECRET
# ===========================================
# Secret partagé avec le Service REST Kobo pour valider les requêtes webhook
KOBO_WEBHOOK_SECRET=change_me_to_a_long_random_string

# ===========================================
# CONFIGURATION JWT
# ===========================================
# Générez une clé secrète forte pour JWT
# Vous pouvez utiliser: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_make_it_very_secure

# ===========================================
# CONFIGURATION MAMP (si utilisé)
# ===========================================
# Port MySQL pour MAMP (généralement 3306)
MAMP_PORT=3306

# ===========================================
# CONFIGURATION CORS
# ===========================================
# URLs autorisées pour CORS (séparées par des virgules)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ===========================================
# CONFIGURATION SMTP (emails)
# ===========================================
# Renseignez votre serveur SMTP pour l’envoi des emails
# Exemple Mailtrap ou autre fournisseur SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
MAIL_FROM_NAME=Lokali
MAIL_FROM_ADDRESS=no-reply@lokali.local
# Optionnel: adresse de réponse
MAIL_REPLY_TO=support@lokali.local

# ===========================================
# CONFIGURATION RATE LIMITING
# ===========================================
# Nombre maximum de requêtes par minute
RATE_LIMIT_MAX=200

# ===========================================
# CONFIGURATION SUPABASE (authentification)
# ===========================================
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ===========================================
# CONFIGURATION DE DÉVELOPPEMENT
# ===========================================
# Activer les logs détaillés
DEBUG=true

# ===========================================
# CONFIGURATION DE PRODUCTION
# ===========================================
# Décommentez et configurez pour la production
# DB_HOST=your_production_db_host
# DB_USER=your_production_db_user
# DB_PASSWORD=your_production_db_password
# DB_NAME=your_production_db_name
# NODE_ENV=production
# JWT_SECRET=your_production_jwt_secret